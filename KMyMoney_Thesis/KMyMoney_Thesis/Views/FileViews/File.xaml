<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="KMyMoney_Thesis.Views.FileViews.File">
     <ContentPage.Content>
    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="Center">
      <!--<ListView x:Name="AccountsListData" HasUnevenRows="True">
        <ListView.ItemTemplate>
          <DataTemplate>         
            <ViewCell>
              <StackLayout HorizontalOptions="Start" >
                <Label Text="{Binding Name, StringFormat='Name = {0}'}"/>
                <Label Text="{Binding Id, StringFormat='Id = {0}'}"/>
                <Label Text="{Binding Type, StringFormat='Type = {0}'}"/>
                <Label Text="{Binding Opened, StringFormat='Opened = {0}'}"/>
                <Label Text="{Binding Currency, StringFormat='Currency = {0}'}"/>
              </StackLayout>
            </ViewCell>           
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>-->
            <ScrollView>

    <Label x:Name="LabelOutput" />
                </ScrollView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>
